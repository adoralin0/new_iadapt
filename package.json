{
  "name": "iadapt-redesign-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build:pages": "node scripts/prepare-pages.js",
    "deploy": "npm run build:pages",
    "fetch:linkedin": "python scripts/fetch_linkedin.py",
    "build:data": "npm run fetch:linkedin"
  },
  "engines": {
    "node": ">=14"
  }
}
